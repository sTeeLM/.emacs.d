;; DMP 歌曲元数据更新模块

(defun dmp-info-init()
  "info模块初始化"
  nil)

(defun dmp-info-cancel-all ()
  "杀死所有更新info的进程"
  nil)

(defun dmp-info-start-batch(&optional id force)
  "启动批量更新"
  nil)

(defun dmp-info-start-oneshot (id &optional force)
  "启动单次更新"
  nil)

(provide 'dmp-info)
